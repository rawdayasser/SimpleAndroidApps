<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rawda.testv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity" />
        <activity android:name=".AlertActivity" />
        <activity android:name=".Progressbar" />
        <activity android:name=".Scroll2Activity" />
        <activity android:name=".ButtonsActivity" />
        <activity android:name=".CheckBoxActivity" />
        <activity android:name=".NoActivity" />
        <activity
            android:name=".BasicActivity"
            android:label="@string/title_activity_basic"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ContextualMenuActivity" />
        <activity android:name=".ContextualActivity_" />
        <activity android:name=".PopupActivity"></activity>
        <activity
            android:name=".droid.DroidMainActivity"
            android:label="@string/title_activity_droid_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".droid.OrderActivity"
            android:label="@string/title_activity_order"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>